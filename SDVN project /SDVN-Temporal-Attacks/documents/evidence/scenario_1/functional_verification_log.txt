FUNCTIONAL VERIFICATION LOG (full-system, real simulation run)
SDVN Temporal-Echo Topology Attack Framework
Generated: 2026-07-21 18:09:14
Governing PDF: /home/sdvn_echo_topology/ns-allinone-3.35/ns-3.35/scratch/Temporal_echo_project (30).pdf
Simulation command: attack_scenario=1, simTime=30s, N_Vehicles=200, N_RSUs=0, N_Controllers=4, attack_percentage=80%
Process exit code: 0
Raw captured stdout: scratch/SDVN project /SDVN-Temporal-Attacks/documents/evidence/scenario_1/ns3_run_raw_stdout.log (1184003 bytes)

====================================================================================================
SUMMARY: 56/56 equations/algorithms implemented in source; 12/56 additionally confirmed executing live during this specific run.
====================================================================================================

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.1: In Eq. 3.1, T is the timestamp manipulation matrix (Tij is the forged timestamp
    source_occurrences=7  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.2: In Eq. 3.2, the left-hand side (t − tfirst ) is the observed age of link eij —how many
    source_occurrences=7  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.3: In Eq. 3.3, τsC (eij , Vi ) is the timestamp written inside the topology report: the
    source_occurrences=6  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.4: In Eq. 3.4, Va ̸= Vb are two distinct vehicles that both reported link eij to the
    source_occurrences=49  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.5: physical MAC senders claim the same identity concurrently; BSHH-S1 (Eq. 3.5)
    source_occurrences=4  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.6: ture, formalized in Eq. 3.6, detects timestamp regression caused by replayed stale
    source_occurrences=2  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.7: that characterises the BSHH attack.The third signature, formalized in Eq. 3.7.
    source_occurrences=11  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.8: ME-S1 (Reporter Count Excess), Eq. 3.8: 2rcomm · λ̂(t) is the expected
    source_occurrences=26  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.9: ME-S2 (Path Diversity Inflation), Eq. 3.9: |P (Vi , Vj )|t is the number of
    source_occurrences=6  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.10: formally defined in Eq. 3.10:
    source_occurrences=7  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.11: ME-S3 (Reporter-Range and Signal Inconsistency), Eq. 3.11: Vk ∈ R(eij , t)
    source_occurrences=17  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.12: In Eq. 3.12, the sum runs over all nine signatures (k = 1, . . . , 9, corresponding to
    source_occurrences=5  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.13: events and |S| = 9 fixed signatures is defined as Eq3.13:
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: PDF '3.13' here is a TABLE-OF-CONTENTS SECTION HEADING ('3.13 Controller-based Multipath Echo Attack (Without RSU)'), not a standalone equation -- confirmed by checking the PDF context directly. The genuine Eq. 3.13 (defining S, the fixed 9-signature set) IS implemented: PEM_WEIGHTS[9] and the 9 detection signatures (TTW-S1/S2/S3, BSHH-S1/S2/S3, ME-S1/S2/S3) in routing.cc, just not cited by this exact equation number in comments.

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.14: ity bound (Eq. 3.14)?
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: PDF '3.14' here is also a TABLE-OF-CONTENTS SECTION HEADING ('3.14 Controller-based Multipath Echo Attack (With RSU)'), sharing the '3.N' numbering space with equations in this PDF's own layout. ME-S4 (malicious controller, with RSU) is implemented in routing.cc (ME_S4_* functions, attack_scenario==12).

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.15: taneously: HMAC integrity (Eq. 3.15), timestamp freshness (Eq. 3.16), and nonce
    source_occurrences=56  runtime_occurrences=4

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.16: taneously: HMAC integrity (Eq. 3.15), timestamp freshness (Eq. 3.16), and nonce
    source_occurrences=23  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.17: novelty (Eq. 3.17). The complete procedure is given in Algorithm 3.
    source_occurrences=43  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.18: as in Eq3.18:
    source_occurrences=16  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.19: subsequent TGN computations—is given in Eq. 3.19. The temporal memory up-
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: Eq. 3.19 defines the temporal graph snapshot Gt = (Vt, Et, Xt, At), the INPUT to Algorithm 2 (FS-DETECT). Implemented via the feature-extraction pipeline in tgn_core.cc, cited there under the adjacent Eq. 3.20 (node feature vector x_v) instead of 3.19 specifically -- confirmed present, citation-number drift only.

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.20: In Eq. 3.20, xv ∈ R5 is the feature vector of node v. The node-identifier embedding
    source_occurrences=21  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.21: Identity mismatch indicator (Eq. 3.21): 1 when the MAC-layer
    source_occurrences=9  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.22: u and v via exponential decay (Eq. 3.22). Stale-timestamped TTW-replayed edges
    source_occurrences=13  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.23: date rule is defined in Eq. 3.23, the neighbourhood aggregation pass in Eq. 3.24,
    source_occurrences=11  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.24: date rule is defined in Eq. 3.23, the neighbourhood aggregation pass in Eq. 3.24,
    source_occurrences=8  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.25: and the anomaly scoring function in Eq. 3.25.
    source_occurrences=9  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.26: predicted by a multi-class head defined in Eq. 3.26:
    source_occurrences=16  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.27: In Eq. 3.27: LBCE is binary cross-entropy between predicted score ŷv and ground-
    source_occurrences=2  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.28: In Eq. 3.28, the left-hand side Verify(σagg , PKagg ) = 1 checks whether the aggregate
    source_occurrences=34  runtime_occurrences=2

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.29: |{Vk : AcceptVk (eij ) = 1}| ≥ t                witnesses to pass Eq. 3.29.
    source_occurrences=47  runtime_occurrences=3

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.30: In Eq. 3.30, σVk is the digital signature produced by reporter Vk over the location-
    source_occurrences=23  runtime_occurrences=2

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.31: and only if all three conditions hold simultaneously and it is defined as Eq3.31:
    source_occurrences=36  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Eq 3.32: independently confirm it and it is defined as in Eq3.32:
    source_occurrences=39  runtime_occurrences=2

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.33: bound (Eq. 3.33). A link remaining active beyond this bound indicates artificial
    source_occurrences=1  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.34: beacons exchanged during this period is given by Eq. 3.34:
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: Eq. 3.34 (N_beacon = L_link / T_b) is a derived quantity feeding the VERIFY_QUORUM check, which IS implemented and explicitly cited as 'Eq. 3.32' throughout routing.cc (Algorithm 4 line 27's own citation, per the code's comments) -- the N_beacon sub-formula itself isn't separately re-cited at its own number.

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.35: by Eq. 3.35:
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: Eq. 3.35 (tau_conv <= T_b + tau_prop) is an analytical convergence-time BOUND used to justify the design's timing margins in the thesis text, not a standalone runtime computation -- there is no discrete 'compute tau_conv' step in Algorithm 1-4 pseudocode. Not expected to appear as a separate code citation.

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.36: window is approximated by Eq. 3.36:
    source_occurrences=3  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.37: t under vehicle density λ(t) is formalized in Eq. 3.37:
    source_occurrences=3  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.38: Node trust score update (Eq. 3.38): incremented by ∆+ on
    source_occurrences=6  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.39: work is partitioned into geographic coverage zones per Eq. 3.39, each assigned a
    source_occurrences=12  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.40: trust update rule is defined in Eq. 3.40:
    source_occurrences=25  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.41: Each controller’s trust score τCj is maintained on the ledger per Eq. 3.41 and is
    source_occurrences=10  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.42: hold, as formalised in Eq. 3.42:
    source_occurrences=13  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.43: as defined in Eq. 3.43:
    source_occurrences=11  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.44: assignment per Eq. 3.44 is triggered, transferring zone management to Ck∗ per
    source_occurrences=8  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.45: detection (Eq. 3.45).
    source_occurrences=1  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.46: on the RSU’s signed beacon evidence record Bnk (t) (Eq. 3.46):
    source_occurrences=21  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.47: k∈Papprove τk        2                      Trust-weighted PBFT acceptance condition (Eq. 3.47).
    source_occurrences=21  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.48: manipulation and is defined in Eq. 3.48:
    source_occurrences=15  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.49: ing trust-weighted PBFT consensus per Eq. 3.49 among RSU Fabric peers, with
    source_occurrences=4  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.50: In Eq. 3.50, the right-hand side exceeds 1 for all f ≥ 1, so the condition reduces
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: Eq. 3.50 is an analytical DERIVATION step ('the right-hand side exceeds 1 for all f>=1, so the condition reduces to tau_B < tau_H') used to justify Eq. 3.42's Byzantine trust threshold in the thesis narrative, not a separate runtime computation.

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Eq 3.51: is constructed as Eq. 3.51:
    source_occurrences=5  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (confirmed via curated manual review, see note)]
    Eq 3.53: filter in Eq. 3.53. The ledger record is retained and monitoring continues.
    source_occurrences=0  runtime_occurrences=0
    MANUAL REVIEW: Eq. 3.53 (P_active membership excludes quarantined/removed nodes) IS implemented -- TrustSelectActivePeers() in routing.cc filters exactly this way -- but the surrounding code cites it as 'Eq. 3.43' (peer-set selection) and 'Eq. 3.46' (E_t^trusted) instead of 3.53 specifically. Citation-number drift only.

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Algorithm 1: node runs the LW signature detector (Algorithm 1) locally on beacons it directly
    source_occurrences=18  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Algorithm 2: receives, and the FS Temporal GNN (Algorithm 2) on its local graph snapshot
    source_occurrences=13  runtime_occurrences=0

[TEST PASSED - IMPLEMENTED & CONFIRMED LIVE AT RUNTIME]
    Algorithm 3: novelty (Eq. 3.17). The complete procedure is given in Algorithm 3.
    source_occurrences=37  runtime_occurrences=1

[TEST PASSED - IMPLEMENTED (not printed during this specific run; scenario/path-dependent)]
    Algorithm 4: contract in Algorithm 4 executes Revoke-Session-Key(Vk ), only the d KEKs
    source_occurrences=34  runtime_occurrences=0

====================================================================================================
SUPPORTING EVIDENCE -- output artifacts genuinely produced by this run
====================================================================================================
[FAIL] PEM per-event log printed
[PASS] TGN detection summary block printed
[PASS] Crypto (TETA-Guard) pipeline initialised
[PASS] LKH tree built
[PASS] Channel/DSRC range table printed
[PASS] Attack scenario configuration banner printed
